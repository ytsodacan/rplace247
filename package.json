{
	"name": "neuro-place",
	"version": "1.0.0",
	"scripts": {
		"build": "mkdir -p dist && cp -r *.html *.js *.css *.md dist/",
		"deploy": "wrangler pages deploy dist --project-name neuro-place"
	},
	"dependencies": {
		"wrangler": "^4.22.0"
	}
}
